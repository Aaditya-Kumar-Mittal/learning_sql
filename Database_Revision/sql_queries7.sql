-- Find nth highest salary in a given dataset

-- SELECT DISTINCT salary FROM table_name ORDER BY salary DESC LIMIT m, n

-- m - reprsents number of rows to skip from beginning
-- n - represents number of rows to fetch after skipping

-- SELECT DISTINCT salary FROM table_name ORDER BY salary DESC LIMIT n-1, 1;